# Copie para .env antes de subir com Docker Compose.
# O .env real nunca entra no repositorio.

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=banco
POSTGRES_PASSWORD=troque-esta-senha
POSTGRES_DB=fila_de_atendimento

PORT=3000
